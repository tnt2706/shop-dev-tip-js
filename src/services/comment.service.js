/* eslint-disable no-await-in-loop */
const { BadRequestError } = require('../core/error.response');
const { Comment } = require('../models');

class InventoryService {
  static async addCommentByUser({ }) {

  }
}

module.exports = InventoryService;
